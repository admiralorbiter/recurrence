# Project Handoff: Recurrence (Continuity Garden)

**Generated**: 2026-08-22 08:40:00Z  
**Project**: `recurrence`  
**Last Promoted Checkpoint**: `CHECKPOINT-E-Q17D` (`fa7ebb809ee98e77da3863eaeaefda1901eb48af`)  
**Operational State**: `IDLE` (Scout K-R2 "Learn the Algebra" Completed)

---

## 1. Moonshot
First causal developmental atlas of an artificial self-model.

---

## 2. Epistemic State Classification

### PROMOTED / ESTABLISHED (Durable Baseline)
- **Q17C**: 2-hop developmental baseline ($16/16$, candidate `b0af2e13e4118564c72b0d004b7e2d54170657d2`).
- **Q17D**: Depth horizon boundary at $k=3$ (candidate `fa7ebb809ee98e77da3863eaeaefda1901eb48af`, classified `NON_MONOTONIC_ANOMALOUS_DEPTH_PROFILE`).

### CANONIZED SCIENTIFIC DISCOVERY (Gate E Frontier)
> **Closure is an algebraic property before it is a memory property.**
>
> The additive recurrent state retains joint relational information nonlinearly ($95.8\%$ with a proper 2-layer MLP probe), but lacks a depth-stable native relational algebra, producing a linear addressability bottleneck.
> A tensor state ($R_t \in \mathbb{R}^{11 \times 11}$) with the canonical contraction law $(h_A h_C^T)(h_C h_D^T) = h_A (h_C^T h_C) h_D^T$ natively supplies closure under update by construction, allowing an accumulated relation to serve as a valid operand for the next experience.

### SCOUT EVIDENCE MILESTONES (Scouts K, K-R1, K-R2)
- **Scout K (Capacity-Matched Tensor Matrix vs Additive State)**:
  - Fixed orthogonal tensor positive control achieves **$16/16$ ($100.0\%$)** $k=2$ and zero-shot $k=3$ recursive composition.
- **Scout K-R1 (Learning Necessity Factorization & Inductive Bias)**:
  - Additive $m_3$ state retains $(u, z)$ endpoint relation with **$95.8\%$ accuracy** ($16/16$ seeds pass) under Xavier-initialized MLP probe, disproving "binding destruction".
  - Untrained isotropic random embeddings (C1) achieve $16/16$ $k=3$ zero-shot composition ($+0.97$ margin), demonstrating that contraction is a sufficient architectural inductive bias.
  - Wrong index contraction ($R^T \cdot E$) yields $0/16$ zero-shot $k=3$ passes (margin $-0.00$), establishing strict algebraic specificity.
- **Scout K-R2 (Learning the Contraction Algebra)**:
  - Initialized with exact uniform operator mixture over 4 contraction topologies $[25.0\%, 25.0\%, 25.0\%, 25.0\%]$ and isotropic random unit embeddings.
  - Exclusively 1- and 2-step developmental experience with broken joins and counterfactuals (zero 3-hop labels).
  - Gradient descent drives canonical $O_1(R \cdot E)$ operator to **$98.2\%$ mean probability** ($16/16$ seeds pass, $\ge 70\%$ dominance in $16/16$ seeds).
  - Applying the learned operator $C_\theta$ recursively at $k=3$ achieves **$16/16$ ($100.0\%$) zero-shot recursive closure** (margin $+0.94$, source drop $+0.94$, destination gap $+0.94$).

---

## 3. Current Frontier & Next Move
- **Next Move**: Draft `CONTRACT-E-Q17E` (Autonomous Contraction Algebra Discovery & Zero-Shot Multi-Hop Recursive Composition).
- **Core Hypothesis**: 2-step developmental experience with broken joins autonomously selects the canonical tensor contraction algebra from a neutral candidate operator space, enabling zero-shot 3-hop composition without 3-hop supervision.

---

## 4. Canonical Evidence & Artifacts
- **Scout K-R2 Telemetry**: [`crates/continuity_garden_core/data/q17e_k_r2_learn_algebra_results.json`](crates/continuity_garden_core/data/q17e_k_r2_learn_algebra_results.json)
- **Scout K-R1 Telemetry**: [`crates/continuity_garden_core/data/q17e_k_r1_learning_necessity_results.json`](crates/continuity_garden_core/data/q17e_k_r1_learning_necessity_results.json)
- **Scout K Telemetry**: [`crates/continuity_garden_core/data/q17e_k_tensor_composition_results.json`](crates/continuity_garden_core/data/q17e_k_tensor_composition_results.json)
