# H0 Guided Static Site

Open `index.html` directly in a browser.

The Markdown files in `../content/` are the narrative source of truth. The HTML pages in this folder are a generated teaching/presentation layer.

No external dependencies or network access are required.
